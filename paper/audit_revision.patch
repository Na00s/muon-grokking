--- a/README.md
+++ b/README.md
@@ -1,17 +1,17 @@
-# Revised submission source
+# Audited submission source
 
-Compile `main.tex` from this directory using Tectonic, or a standard LaTeX/BibTeX installation. All figure PDFs, table sources, bibliography files, and the supplied conference style are included.
+Compile `main.tex` in this directory with Tectonic or a standard LaTeX/BibTeX installation. Figures, tables, bibliography, and the supplied conference style are included.
 
 ```sh
 tectonic main.tex
 ```
 
-The revision preserves the supplied title, anonymous author block, conference template, six original figure PDFs, and 17 original table files. The T1 font-encoding declaration preserves the requested Times face with Tectonic. New material is in Section 4.4, Figure 4, and Appendix J (including Figure 8). Existing causal and spectral interpretations are corrected where affected by these results.
+The revision preserves the title, anonymous authors, section order, all six original figure PDFs, and the numerical cells in the 17 original appendix table files. Captions, definitions, analysis, and claims are revised where needed. New material appears in Section 4.4, Figure 4, and Appendix J, including Figure 8.
 
-The two new figures were generated from recorded measurements. `figures/followup_figure_provenance.json` contains every plotted value and the hashes of 15 input result files. To regenerate them, install NumPy and Matplotlib and point the plotting script to the released follow-up results tree, which contains the `work/` subdirectory:
+The two new figures use recorded experimental measurements. Their complete values and the hashes of 15 input files are in `figures/followup_figure_provenance.json`. To regenerate from the released follow-up results tree:
 
 ```sh
 python figures/build_followup_figures.py --workspace /path/to/followup_studies --output .
 ```
 
-The existing figure PDFs require no regeneration to compile the paper. The plotting script verifies preservation of all six original figure files.
+From the repository layout, the script also resolves the data and output paths automatically. In a standalone source archive, pass the data root explicitly. The script checks that the original figures are preserved. The source archive is sufficient to compile the paper without downloading experimental binaries.
--- a/appendix.tex
+++ b/appendix.tex
@@ -4,11 +4,11 @@
 \input{tables/definitions}
 \input{tables/configs}
 
-Each example is the token sequence $[a,b,=]$; the vocabulary contains $p+1=114$ tokens and the output layer $p=113$ classes. The ordered operand grid contains $p^2=12{,}769$ examples; a fixed random $30\%$ (unless stated) forms the training set, generated once from the run seed and shared by all matched comparisons. The original runs load a saved initial state from disk, evaluate every $100$ steps, and checkpoint every $1{,}000$. Depth-1 and depth-2 runs last $100{,}000$ steps and depth-4 runs $300{,}000$.
+Each example is the token sequence $[a,b,=]$; the vocabulary contains $p+1=114$ tokens and the output layer $p=113$ classes. The ordered operand grid contains $p^2=12{,}769$ examples; a fixed random $30\%$ (unless stated) forms the training set, generated once from the run seed and shared by all matched comparisons. The original sweeps load saved initial states, evaluate every $100$ steps, and checkpoint every $1{,}000$. Their budgets are $100{,}000$ steps at depths 1 and 2 and $300{,}000$ at depth 4. Diagnostic branches use the shorter horizons and finer grids specified below.
 
-Each block holds four two-dimensional matrices (fused QKV $384\times128$, attention output $128\times128$, MLP input $512\times128$, MLP output $128\times512$), $196{,}608$ parameters; with embeddings and unembedding the depth-1 model has $226{,}048$ parameters. Depth is varied by appending blocks under nested initialization, so deeper models begin from a strict superset of the shallower model's initial state. Muon maintains an exponential moving average of the gradient with $\beta=0.95$, forms a Nesterov-style update, normalizes it by its Frobenius norm, applies five quintic Newton--Schulz iterations with coefficients $(3.4445,-4.7750,2.0315)$, scales by $\sqrt{\max(1,\text{rows}/\text{cols})}$, and applies decoupled weight decay; the implementation refuses any parameter that is not a matrix. All AdamW groups use $\beta_1=0.9$, $\beta_2=0.999$.
+Each block holds four two-dimensional matrices (fused QKV $384\times128$, attention output $128\times128$, MLP input $512\times128$, MLP output $128\times512$), $196{,}608$ parameters; with embeddings and unembedding the depth-1 model has $226{,}048$ parameters. Depth is varied by appending blocks under nested initialization, so deeper models begin from a strict superset of the shallower model's initial state. Muon maintains an exponential moving average of the gradient with $\beta=0.95$, forms a Nesterov-style update, divides it by its Frobenius norm plus $10^{-7}$, applies five quintic Newton--Schulz iterations with coefficients $(3.4445,-4.7750,2.0315)$, scales by $\sqrt{\max(1,\text{rows}/\text{cols})}$, and applies decoupled weight decay; the implementation refuses any parameter that is not a matrix. All AdamW groups use $\beta_1=0.9$, $\beta_2=0.999$.
 
-Table~\ref{tab:definitions} gives the operational definitions for the original experiments; evaluations occur every $100$ steps, so a sustained criterion holds across a $500$-step window. Training sweeps and matched-branch experiments run at a single seed on an accelerator backend that is not bitwise deterministic across processes; the five-seed replications run on CPU, where execution is bitwise deterministic, so the Muon and Stable Muon arms are the same trajectory until the freeze fires and agree to a maximum absolute difference of zero over the seventy-four to seventy-five evaluations preceding it. The seed sets the initialization and the train/test split together, so the five seeds are five draws of the problem instance.
+Table~\ref{tab:definitions} gives the operational definitions for the original experiments; evaluations occur every $100$ steps, so a sustained criterion holds across a $400$-step span. Training sweeps and matched-branch experiments run at a single seed on an accelerator backend that is not bitwise deterministic across processes; the five-seed replications run on CPU, where execution is bitwise deterministic, and the Muon and Stable Muon arms have identical logged metrics over the seventy-four to seventy-five evaluations preceding the freeze. This check compares recorded metrics; it does not compare every parameter tensor at each evaluation. The seed sets the initialization and the train/test split together, so the five seeds are five draws of the problem instance.
 
 \section{Sweeps, baseline selection, and generality}
 \label{app:sweeps}
@@ -17,9 +17,9 @@
 \input{tables/muon-sweep}
 \input{tables/generality}
 
-Tables~\ref{tab:adamw-sweep} and~\ref{tab:muon-sweep} give both sweeps under identical architecture, split, and budget. The fastest AdamW configuration ($\mathrm{lr}=10^{-2}$, $\mathrm{wd}=1.0$) is unusable: $918$ sub-threshold evaluations at this seed, and across seeds three of five never reach sustained $95\%$, the two that do record $805$ and $956$, and all five end within a tenth of a point of chance. The baseline is therefore the fastest configuration that is strictly stable in this sweep, $\mathrm{lr}=10^{-3}$, $\mathrm{wd}=3.0$; the seed replication shows strict stability at that setting is a property of one seed rather than of the configuration. For Muon no configuration is strictly stable, so the selected configuration is the fastest.
+Tables~\ref{tab:adamw-sweep} and~\ref{tab:muon-sweep} give both sweeps under identical architecture, split, and budget. The fastest AdamW configuration ($\mathrm{lr}=10^{-2}$, $\mathrm{wd}=1.0$) is unusable: $918$ sub-threshold evaluations at this seed, and across seeds three of five never reach sustained $95\%$, the two that do record $805$ and $956$, and all five end near chance. The baseline is therefore the fastest configuration that is strictly stable in this sweep, $\mathrm{lr}=10^{-3}$, $\mathrm{wd}=3.0$; the seed replication shows strict stability at that setting is a property of one seed rather than of the configuration. For Muon no configuration is strictly stable, so the selected configuration is the fastest.
 
-Both optimizers fit the training set between steps $200$ and $1{,}400$ in every grokking configuration, so the difference lies entirely in the delay from memorization to generalization: $30{,}100$ steps for AdamW against $12{,}544$ for Muon averaged over successes. Across five seeds Muon reaches sustained generalization between $5{,}300$ and $5{,}400$ steps and the AdamW reference between $4{,}000$ and $11{,}700$, medians $5{,}400$ and $8{,}300$.
+Both optimizers fit the training set between steps $200$ and $800$ in every grokking configuration, so the difference lies almost entirely in the delay from memorization to generalization: $30{,}100$ steps for AdamW against $12{,}544$ for Muon averaged over successes. Across five seeds Muon reaches sustained generalization between $5{,}300$ and $5{,}400$ steps and the AdamW reference between $4{,}000$ and $11{,}700$, medians $5{,}400$ and $8{,}300$.
 
 Varying the readout learning rate over $10^{-4}$, $2.5\times10^{-4}$, and $5\times10^{-4}$ gives post-grokking minima of $37.13\%$, $79.40\%$, and $3.81\%$; varying its weight decay over $0$, $0.1$, and $0.5$ gives $33.34\%$, $0.79\%$, and $1.12\%$; every setting is unstable. On $(a-b)\bmod 113$ the Muon run records $18$ post-grokking evaluations below $95\%$ with a minimum of $1.52\%$ and the AdamW run records none, reproducing the depth-1 addition asymmetry at $194$ and none. Table~\ref{tab:generality} varies modulus, training fraction, and width; Stable Muon and Muon share a configuration up to the freeze, so differences in their grokking steps bound run-to-run variation on this backend.
 
@@ -28,21 +28,21 @@
 
 \input{tables/quiet}
 
-Table~\ref{tab:quiet} follows the $691$ steps preceding the collapse of \S\ref{sec:quiet} and the failure interval itself. On the hidden group the gradient-driven component is $0.1499$ at step $44{,}100$ against a decay component of $0.1475$, a ratio of $0.984$, leaving a net displacement of $0.0462$ that grows to $0.0759$ across the window while the orthogonalized step stays flat; over the window the hidden gradient norm grows by $3.92$ while its gradient-driven step changes by $0.97$, and the non-hidden gradient grows by $2.60$ while the embedding and readout steps grow by $2.65$ and $2.84$. The readout is pulled by decay rather than by the loss on $14.9\%$ of quiet-window steps. The instantaneous per-parameter displacement ratio between the groups falls from $16.2$ to $3.3$ across the window. At the failure the readout gradient norm rises from $3.52\times10^{-5}$ to $836.10$; the orthogonalized Muon step over that interval is $0.1429$, lower than the $0.1455$ preceding it, and its maximum over the branch occurs after the event.
+Figure~\ref{fig:quiet} uses the every-step instrumented rerun, with a quiet window from $44{,}010$ through $44{,}700$ and failure at $44{,}702$. Table~\ref{tab:quiet} uses a separate ten-step spectral replay, whose first observed failure is at $44{,}710$. The following step/decay diagnostics use the instrumented rerun. On the hidden group the gradient-driven component is $0.1499$ at step $44{,}100$ against a decay component of $0.1475$, a ratio of $0.984$, leaving a net displacement of $0.0462$ that grows to $0.0759$ across the window while the orthogonalized step stays flat; over the window the hidden gradient norm grows by $3.92$ while its gradient-driven step changes by $0.97$, and the non-hidden gradient grows by $2.60$ while the embedding and readout steps grow by $2.65$ and $2.84$. The readout decay component has larger norm than its gradient-driven component on $14.9\%$ of quiet-window steps. In the ten-step spectral replay, the combined non-hidden gradient norm rises from $3.52\times10^{-5}$ to $836.10$; the orthogonalized Muon step over that interval is $0.1429$, lower than the $0.1455$ preceding it, and its maximum over the branch occurs after the event.
 
 \section{Ablating the normalized-orthogonalization path}
 \label{app:ablation}
 
 \input{tables/nons}
 
-The ablation removes the Frobenius normalization and the Newton--Schulz iteration together, leaving momentum, shape scaling, and weight decay; the update carries the magnitude and direction of the buffer, and the learning rate must rise to $0.3$ or $1.0$ for the model to learn at all. Table~\ref{tab:nons}: six ablated runs generalize, none records a post-grokking evaluation below $95\%$, and all six end in a non-finite loss, in float64 as well as float32, at terminal steps spanning $22{,}889$ to $38{,}955$ and at hidden norms that set no failure threshold. Muon on the matched run reaches a minimum of $29.49\%$ and recovers to $96.48\%$, with thirteen sub-threshold evaluations before the earliest ablated failure: the ablated runs span the window in which Muon oscillates and none of them does. The two failures differ in kind, and the ablation collapses spectral dispersion from $326.09$ effective conjugate pairs to $4.11$ while leaving the task-aligned family at $100\%$ sufficiency (Appendix~\ref{app:dispersion}).
+The ablation removes Frobenius normalization and the Newton--Schulz iteration together, retaining momentum, shape scaling, and weight decay. In the tested sweep, learning requires a rate of $0.3$ or $1.0$. Six ablated runs generalize, with no recorded post-grokking evaluation below $95\%$ (Table~\ref{tab:nons}). All six were observed to terminate with non-finite loss, in float32 and float64, at steps $22{,}889$--$38{,}955$. The matched Muon comparator records thirteen sub-threshold evaluations through step $22{,}800$. Over its full $100{,}000$ steps, its minimum and final accuracies are $29.49\%$ and $96.48\%$. At the analyzed checkpoints, ablation reduces dispersion from $326.09$ effective conjugate pairs to $4.11$, while the task-aligned family retains $100\%$ sufficiency (Appendix~\ref{app:dispersion}).
 
 \section{Prevention details}
 \label{app:prevention}
 
 \input{tables/stable}
 
-In four of the five runs the freeze is triggered automatically, $2{,}000$ steps after the first evaluation of a five-evaluation streak at or above $95\%$; the main-condition run uses a fixed step. Across all five the freeze lands $2{,}100$ to $2{,}200$ steps after sustained generalization. Within a depth-2 run the median step costs $57.87$\,ms before the freeze and $54.72$ after; at depth 4, $111.39$ and $107.75$; pre-freeze rates match ordinary Muon's. Holding only the unembedding constant from the same step leaves the run unstable, minimum $83.13\%$ and eighteen of $936$ evaluations below $95\%$; freezing the embeddings and unembedding together at the same step produces none.
+In four of the five runs the freeze is triggered automatically, $2{,}000$ steps after the first evaluation of a five-evaluation streak at or above $95\%$; the main-condition run uses a fixed step. The first logged frozen evaluation lands $2{,}100$ to $2{,}200$ steps after sustained generalization; the actual freezes occur $100$ steps earlier. Within a depth-2 run the median step costs $57.87$\,ms before the freeze and $54.72$ after; at depth 4, $111.39$ and $107.75$; pre-freeze rates match ordinary Muon's. Holding only the unembedding constant from the same step leaves the run unstable, minimum $83.13\%$ and eighteen of $936$ evaluations below $95\%$; freezing the embeddings and unembedding together at the same step produces none.
 
 \section{Fourier decomposition and intervention suite}
 \label{app:fourier}
@@ -51,23 +51,23 @@
 \input{tables/interventions}
 \input{tables/families-result}
 
-For a chosen point in the forward pass the model runs on all $p^2$ operand pairs, the states form a tensor $X\in\mathbb{R}^{p\times p\times d}$, and an orthonormal two-dimensional DFT over the operand axes gives $\widehat{X}[k,l,:]$ with power $\lVert\widehat{X}[k,l,:]\rVert^2$. Because $X$ is real, modes occur in conjugate pairs and every filter retains or removes both members together; transforms run in single precision on CPU. The families of Table~\ref{tab:families} are pairwise disjoint and exhaust the grid, and addition and subtraction are disjoint because $p$ is odd. Power fractions are normalized against non-constant power; filters that retain the addition family retain the full diagonal including the constant mode, since the bias-free unembedding turns a constant component into a fixed logit offset. Removing a family and retaining its complement are the same measurement, and they agree exactly everywhere, checking the filters.
+For a chosen point in the forward pass the model runs on all $p^2$ operand pairs, the states form a tensor $X\in\mathbb{R}^{p\times p\times d}$, and an orthonormal two-dimensional DFT over the operand axes gives $\widehat{X}[k,l,:]$ with power $\lVert\widehat{X}[k,l,:]\rVert^2$. Because $X$ is real, modes occur in conjugate pairs and every filter retains or removes both members together; transforms run in single precision on CPU. The families of Table~\ref{tab:families} are pairwise disjoint and exhaust the grid, and addition and subtraction are disjoint because $p$ is odd. Power fractions are normalized against non-constant power; every non-constant-family sufficiency filter also retains the constant mode, which becomes a fixed logit offset under the bias-free unembedding. Ablation removes only the selected non-constant modes, retaining this offset. The two masks are complementary on the non-constant spectrum and share the constant mode. In the margin decomposition and $\alpha$ sweep, the task-aligned component excludes the constant mode, which belongs to the remainder. Solved-checkpoint selection and intervention accuracies use the full operand grid; training trajectories explicitly labeled test accuracy use the held-out split.
 
-Table~\ref{tab:families-result} applies sufficiency and ablation to each family at forty-three solved checkpoints spanning five seeds and the Muon, Stable Muon, and AdamW regimes. Equal-power relocation ranges $0.88\%$ to $15.04\%$ over $430$ replicates. Taken in descending order of power, reaching $95\%$ requires two to four conjugate pairs at AdamW checkpoints (median $3$), against three to seventeen at Muon (median $12.5$) and eight to nineteen at Stable Muon (median $11$). Along the AdamW trajectory of \S\ref{sec:outvoted}, the five strongest pairs give $4.91\%$ at step $3{,}000$, $19.48\%$ at $20{,}000$, $70.73\%$ at $30{,}000$, and $100\%$ at $37{,}000$, while the family's share rises from $1.05\%$ through $11.08\%$ and $33.20\%$ to $70.62\%$, reaching $90.51\%$ by step $100{,}000$; test accuracy over those steps is $0.30\%$, $2.08\%$, $15.97\%$, and $100\%$.
+Table~\ref{tab:families-result} applies sufficiency and ablation to each family at forty-three solved checkpoints spanning five seeds and the Muon, Stable Muon, and AdamW regimes. Equal-power relocation ranges $0.88\%$ to $15.04\%$ over $430$ replicates. Among the tested descending-power prefixes, the smallest reaching $95\%$ contains two to four conjugate pairs at AdamW checkpoints (median $3$), against three to seventeen at Muon (median $12.5$) and eight to nineteen at Stable Muon (median $11$). Along the AdamW trajectory of \S\ref{sec:outvoted}, the five strongest pairs give $4.91\%$ at step $3{,}000$, $19.48\%$ at $20{,}000$, $70.73\%$ at $30{,}000$, and $100\%$ at $37{,}000$, while the family's share rises from $1.05\%$ through $1.66\%$ and $11.08\%$ to $70.62\%$, reaching $90.51\%$ by step $100{,}000$; test accuracy over those steps is $0.30\%$, $0.29\%$, $2.08\%$, and $100\%$.
 
 \section{Spectral dispersion}
 \label{app:dispersion}
 
 \input{tables/sparsity}
 
-Table~\ref{tab:sparsity} reports the inverse participation ratio over all $6{,}384$ non-constant conjugate pairs and the task-aligned family's power share on matched depth-1 runs. Across the five-seed study AdamW occupies $4.40$ effective pairs on average over thirty solved checkpoints, Muon $385.85$ over twenty-five, and Stable Muon $343.96$ over thirty-six: freezing the interface leaves the dispersion where Muon put it and removes the instability anyway. Within the family's $56$ pairs, AdamW occupies $1.99$ to $2.59$ effective pairs across the generality variants and Muon $21.33$ to $55.34$. Each operation supplies a family that provably computes nothing on the other's task: AdamW places $0.0$--$0.3\%$ of non-constant power there and Muon $2.5$--$17.7\%$.
+Table~\ref{tab:sparsity} reports the entropy effective count $\exp(-\sum_i q_i\log q_i)$ over all $6{,}384$ non-constant conjugate pairs and the task-aligned family's power share on matched depth-1 runs. Here $q_i$ denotes normalized pair power. Across the five-seed study AdamW occupies $4.40$ effective pairs on average over thirty solved checkpoints, Muon $385.85$ over twenty-five, and Stable Muon $343.96$ over thirty-six: freezing the interface leaves the dispersion where Muon put it and removes the instability anyway. Within each family's $(p-1)/2$ pairs, the generality analysis instead uses the inverse participation ratio $1/\sum_i q_i^2$, with $q_i$ renormalized within the family: $1.99$--$2.59$ for solved AdamW variants and $21.33$--$55.34$ for Muon. On the uniform operand grid, a predictor using only the opposite family and a constant offset has accuracy exactly $1/p$, since sum and difference are independent for odd $p$. Across solved matched checkpoints, AdamW places $0.0$--$7.8\%$ of non-constant power there and Muon $2.5$--$17.7\%$.
 
 \section{The matched branch through failure, recovery, and masking}
 \label{app:branchtraj}
 
 \input{tables/coordinates}
 
-Table~\ref{tab:coordinates} follows the family's top-twenty membership, power share, isolated accuracy, and full-model accuracy along the depth-4 matched pair of \S\ref{sec:outvoted} from a bit-identical state. The frozen branch holds all three quantities for $174{,}000$ steps; the control loses all three at filtered-pathway failure through its current readout, recovers all three, and loses the share again at circuit masking while the isolated family still gives $100\%$. The $\alpha$-sweep crossing at $\alpha=2.55$ occurs at a share of $0.6247$, below the frozen branch's native $0.9069$. Replaying a depth-1 collapse and recovery at ten-step resolution: full accuracy returns to $99.90\%$ within $300$ steps while the addition-family power cosine to the pre-collapse state reaches only $0.55$; the pre-collapse representation holds between $4.2\%$ and $5.7\%$ under the recovered readout throughout, while the recovered representation reads at $90.73\%$ under the pre-collapse readout. The matched branches' five strongest modes are identical at the branch point and share none by steps $160{,}000$ through $230{,}000$. Substituting the frozen branch's addition component into the control under the frozen readout gives $99.99\%$; under the control's own readout, $5.05\%$; the frozen branch's full final representation gives $97.61\%$ and $4.46\%$ under the two readouts; substituting only the frozen branch's non-addition component into the control gives $36.79\%$, below the control's unmodified $45.85\%$.
+Table~\ref{tab:coordinates} follows the family's top-twenty membership, power share, isolated accuracy, and full-model accuracy along the depth-4 matched pair of \S\ref{sec:outvoted} from a bit-identical state. Over $174{,}000$ steps, the frozen branch retains high filtered accuracy and substantial family power; the control loses all three at filtered-pathway failure through its current readout, recovers all three, and loses the share again at circuit masking while the isolated family still gives $100\%$. The $\alpha$-sweep crossing at $\alpha=2.55$ occurs at a share of $0.6247$, below the frozen branch's native $0.9069$. Replaying a depth-1 collapse and recovery at ten-step resolution: full accuracy returns to $99.90\%$ within $300$ steps while the addition-family power cosine to the pre-collapse state reaches only $0.55$; the pre-collapse representation holds between $4.2\%$ and $5.7\%$ under the recovered readout throughout, while the recovered representation reads at $90.73\%$ under the pre-collapse readout. The matched branches' five strongest modes are identical at the branch point and share at most one pair at sampled steps $160{,}000$ through $230{,}000$. Patching the addition component of the frozen branch's final MLP write into the control under the frozen readout gives $99.99\%$; under the control's own readout, $5.05\%$; patching its full final MLP write gives $97.61\%$ and $4.46\%$ under the two readouts; patching only the non-addition component of that MLP write into the control gives $36.79\%$, below the control's unmodified $45.85\%$.
 
 \section{Depth}
 \label{app:depth}
@@ -77,4 +77,6 @@
 \input{tables/depthreadout}
 \input{tables/FIG_depth}
 
-Muon reaches sustained generalization in $2{,}300$ steps at depth 2 against $5{,}100$, and in $52{,}600$ at depth 4 where the baseline peaks at $96.69\%$ and never sustains the threshold within $300{,}000$ steps; Table~\ref{tab:wallclock} gives the comparison in steps and elapsed time, with a Muon step costing $1.75$--$1.80\times$ an AdamW step. Table~\ref{tab:stagewise}: zeroing the block-2 or block-3 attention write leaves $0.94\%$ and $0.44\%$, while relocating the family or scrambling its phase at those stages costs nothing; sensitivity appears abruptly at the block-3 MLP activation and every later stage is at or below chance. The depth-2 checkpoint behaves identically with the transition at the block-1 MLP activation. Across the eleven solved checkpoints of the depth sweep the first stage where pair-global phase scrambling costs more than half the accuracy is an MLP activation or write in ten, and an attention write in none. Table~\ref{tab:depthreadout}: direct decoding reaches $95\%$ only at the final block across all $41$ generalizing checkpoints, never exceeding $17.97\%$ earlier; filtering to the family raises accuracy at sixteen of seventeen partially aligned residual states, by $4.88$ points on average, so the competition of \S\ref{sec:outvoted} is visible along the forward pass and not only across training. Comparing the depth-4 matched arms at every checkpoint after their shared state, the power spectral cosine between them stays above $0.98$ in the first block and falls to $0.03$--$0.44$ in the fourth at fourteen of nineteen: the branches part company in the block that feeds the readout. The exact-state matched branch at step $126{,}200$ raises mean post-branch accuracy from $48.07\%$ to $97.50\%$, final accuracy from $36.88\%$ to $98.87\%$, and cuts evaluations below $90\%$ from $1{,}353$ to $66$.
+Muon reaches sustained generalization in $2{,}300$ steps at depth 2 against $5{,}100$, and in $52{,}600$ at depth 4 where AdamW peaks at $96.69\%$ and never sustains the threshold within $300{,}000$ steps. Table~\ref{tab:wallclock} compares steps and elapsed time; a Muon step costs $1.75$--$1.80\times$ an AdamW step. At the depth-4 checkpoint in Table~\ref{tab:stagewise}, zeroing the block-2 and block-3 attention writes leaves $0.94\%$ and $0.44\%$ accuracy. Frequency relocation and phase scrambling at these writes preserve near-baseline accuracy. Both perturbations cause a large drop at the block-3 MLP activation. Figure~\ref{fig:depth} and Table~\ref{tab:stagewise} show the reported perturbation realization. Later-stage outcomes vary across the five perturbations, so near-chance accuracy in this realization does not establish uniform sensitivity across perturbations.
+
+Across eleven solved checkpoints, the first stage where mean pair-phase scrambling costs more than half the baseline accuracy is an MLP activation in ten and an MLP write in one. This criterion averages five perturbations per stage. Direct decoding reaches $95\%$ only at the final block across all $41$ checkpoints selected by full-grid accuracy, never exceeding $17.97\%$ earlier (Table~\ref{tab:depthreadout}). Within the eleven-checkpoint stage-intervention subset, among seventeen residual states with direct-readout accuracy between $2\%$ and $95\%$, family filtering improves sixteen, with a mean increase of $4.88$ percentage points across all seventeen. Across eighteen depth-4 checkpoints after the shared state, between-branch power cosine stays above $0.98$ in the first block; in the fourth it lies between $0.03$ and $0.44$ at fifteen checkpoints. The exact-state branch at step $126{,}200$ raises mean post-branch accuracy from $48.07\%$ to $97.50\%$, final accuracy from $36.88\%$ to $98.87\%$, and cuts evaluations below $90\%$ from $1{,}353$ to $66$.
--- a/figures/build_followup_figures.py
+++ b/figures/build_followup_figures.py
@@ -1,7 +1,8 @@
 """Plot the recorded follow-up results; this script performs no model fitting.
 
-Run with the research Python environment from any working directory. The default
-workspace is inferred from this file's location; --workspace supports relocation.
+Run with the research Python environment from any working directory. The default data root is the repository's followup_studies directory. A standalone
+source archive requires --workspace /path/to/followup_studies; that directory must
+contain work/. The default output directory is the paper source directory.
 The adjacent swaps, selected decoder snapshots, and matched arithmetic branches
 reuse the same five seeds. No panel reports independent replicate counts for
 multiple interventions on the same seed, and no statistical intervals are fitted.
@@ -25,9 +26,16 @@
 
 def main():
     parser = argparse.ArgumentParser(description=__doc__)
-    parser.add_argument("--workspace", type=Path, default=HERE.parents[2])
-    parser.add_argument("--output", type=Path, default=HERE.parent / "revised")
+    candidate = HERE.parents[2] / "followup_studies"
+    parser.add_argument("--workspace", type=Path, default=candidate if (candidate / "work").is_dir() else None,
+                        help="Released followup_studies directory containing work/; required outside the repository.")
+    parser.add_argument("--output", type=Path, default=HERE.parent,
+                        help="Paper source directory containing the six original figures.")
     args = parser.parse_args()
+    if args.workspace is None:
+        parser.error("Supply --workspace /path/to/followup_studies (the directory containing work/).")
+    if not (args.workspace / "work").is_dir():
+        parser.error("--workspace must contain the released work/ results directory.")
     root, output = args.workspace.resolve(), args.output.resolve()
     output.mkdir(parents=True, exist_ok=True)
     sources = {}
--- a/followup_appendix.tex
+++ b/followup_appendix.tex
@@ -6,7 +6,7 @@
 \subsection{Monitoring and matched states}
 \label{app:followup-protocol}
 
-The causal branches and fresh baseline replications use a joint event: training and test accuracy must both fall below $90\%$ after sustained grokking. Training accuracy is checked at every update; test accuracy is checked every $100$ updates and at every post-grokking state with training accuracy below $90\%$. Thus every joint event is detected, while brief test-only excursions can fall between evaluations. Six consecutive grid evaluations at or above $95\%$ confirm grokking. The confirmation step is the \emph{last} evaluation of that streak, $500$ updates after the streak-start definition used for grokking speed in \S\ref{sec:setup}.
+The causal branches and fresh baseline replications use a joint event: training and test accuracy must both fall below $90\%$ after sustained grokking. Training accuracy is checked at every update; test accuracy is checked every $100$ updates and at every post-grokking state with training accuracy below $90\%$. Thus every joint event is detected, while brief test-only excursions can fall between evaluations. Six consecutive grid evaluations at or above $95\%$ confirm grokking. The confirmation step is the \emph{last} evaluation of this six-check streak. Original sweep speeds instead report the start of a five-check streak (\S\ref{sec:setup}); the criteria therefore differ as well as their reporting points.
 
 Within each seed, matched arithmetic branches restore identical parameters, optimizer buffers, and random-number-generator state. Exact next-update replays reproduce all three. The original prefixes combine historical evaluation grids with densely captured failures; their positive events establish binary incidence. We therefore compare event presence, without comparing their counts, durations, or historical minima against the uniformly monitored branches. The five trajectories were selected for mechanistic study and give no estimate of collapse prevalence over an unrestricted seed population.
 
@@ -31,7 +31,7 @@
 \subsection{Fresh decoders and the basis hypothesis}
 \label{app:basis}
 
-Fresh bias-free linear decoders are fitted to the raw final residual using only the original $3{,}830$ training examples. An uncentered SVD preconditioner retains all $128$ dimensions, with singular values floored at $10^{-6}$ of the largest. Each inner fit estimates its own preconditioner. A $20\%$ split of the training set selects the $L_2$ penalty from $\{0,10^{-8},10^{-6},10^{-4},10^{-2}\}$ by validation cross-entropy; the selected decoder is then refitted on all training examples. The $8{,}939$ test examples enter only the final evaluation. Fits use float64 logistic regression and L-BFGS-B; convergence and unregularized controls are recorded.
+Fresh bias-free linear decoders are fitted to the raw final residual using only the original $3{,}830$ training examples. An uncentered SVD preconditioner retains all $128$ dimensions, with singular values floored at $10^{-6}$ of the largest. Each inner fit estimates its own preconditioner. A $20\%$ split of the training set selects an $L_2$ penalty on the preconditioned coefficients from $\{0,10^{-8},10^{-6},10^{-4},10^{-2}\}$ by validation cross-entropy; the selected decoder is then refitted on all training examples. The $8{,}939$ test examples enter only the final evaluation. Fits use float64 logistic regression and L-BFGS-B; convergence and unregularized controls are recorded.
 
 At the selected collapsed checkpoints, decoder accuracies for seeds $0$--$4$ are $98.72\%$, $98.20\%$, $100.00\%$, $98.77\%$, and $100.00\%$, against $99.51\%$, $99.56\%$, $100.00\%$, $99.40\%$, and $100.00\%$ at their references. Probe checkpoints are steps $17{,}494$, $17{,}721$, $18{,}069$, $16{,}309$, and $16{,}060$: the last two occur after the adjacent transitions in Table~\ref{tab:adjacent}. These results establish substantial surviving linear task information; the small gaps in three seeds leave room for reduced decodability or probe limitations.
 
@@ -57,7 +57,7 @@
 
 Positive controls plant exact residual gauges into the trained network. Eight controls cover condition numbers $1$, $3$, $10$, and $100$ in float32 and float64; twenty-four further controls plant observed fitted matrices with their measured orientation and conditioning. Exact compensation and train-fitted compensation recover $100\%$ in every fixed-condition control. For the three fresh adjacent transitions, observed forward-map errors are $4.65\%$, $1.10\%$, and $10.77\%$, against $0.0004$--$0.0006\%$ for their planted controls. These errors reject exact global equivalence for the measured pairs. Healthy evolution also departs from exact equivalence (Table~\ref{tab:followup-geometry}).
 
-Approximate transport remains useful: inverse-linear compensation reaches $95.13\%$, $100\%$, and $94.37\%$ at the three fresh first events. Reference timing matters. Seed $0$'s head from step $17{,}200$ scores $14.05\%$ on the collapsed residual, while the head from $17{,}493$ scores $96.39\%$. Its geometric discrepancy from the distant reference is already present before the final failing update. Moreover, the complete real task-only Fourier coefficient matrices have shape $113\times128$; when both have full row rank, an invertible map between them always exists. A fitted task-only map can therefore align that projection exactly while scoring only $4.62\%$ on raw held-out features.
+Approximate transport remains useful: inverse-linear compensation reaches $95.13\%$, $100\%$, and $94.37\%$ at the three fresh first events. Reference timing matters. Seed $0$'s head from step $17{,}200$ scores $14.05\%$ on the collapsed residual, while the head from $17{,}493$ scores $96.39\%$. Its geometric discrepancy from the distant reference is already present before the final failing update. Moreover, the complete real task-only Fourier coefficient matrices have shape $113\times128$; when both have full row rank, an invertible map between them always exists. For the seed-$4$ generalization-drop pair at steps $16{,}000$ and $17{,}000$, a fitted task-only map aligns that projection exactly while scoring $4.62\%$ on raw held-out features. This pair is separate from the adjacent failures above.
 
 One-time repairs also have limited persistence. Eighteen continuations resume two captured failures for $500$ updates under nine head/state interventions each. Every successful repair retaining optimizer state falls below $90\%$ training accuracy on the next update. Clearing the head's Adam state improves eventual recovery but permits the immediate relapse. These tests separate instantaneous decodability from stability under continued optimization.
 
@@ -86,9 +86,9 @@
 
 The specificity study branches each seed at step $15{,}000$ and runs to $20{,}000$ with three corrections. Accurate CE gives $0/5$ joint failures; target-derivative repair gives $0/5$; per-example zero-sum projection gives $0/5$. Every original trajectory has a captured failure in this window. All fifteen corrected branches finish at $100\%$ test accuracy; the minimum measured test accuracy across these branches is $99.978\%$.
 
-For stock logit gradient $g$, target repair retains each wrong-class entry and sets $g_y=-\sum_{j\ne y}g_j$, leaving the reported forward loss unchanged. Projection replaces $g$ by $g-\mathbf{1}(\mathbf{1}^{\top}g)/p$. Derivative errors are relative $L_2$ errors against the accurate float64 derivative evaluated on the identical float32 logits. The readout-gradient reference casts this logit derivative to float32 before backpropagating through the shared float32 network. At the fixed seed-$4$, step-$16{,}000$ audit, target repair reduces relative derivative error from $32.02\%$ to $4.19\times10^{-7}$, whereas projection leaves $31.88\%$. At step $20{,}000$, projected derivatives retain $52.94$--$86.07\%$ relative error despite a class-sum residual below $2.1\times10^{-8}$ relative to the reference norm. Thus restoring the class-sum identity can prevent the observed failures over this window even with substantial remaining derivative error. Numerical feature inflation and this projection control build on \citet{liu2026glitching}.
+For stock logit gradient $g$, target repair retains each wrong-class entry and sets $g_y=-\sum_{j\ne y}g_j$, leaving the reported forward loss unchanged. Projection replaces $g$ by $g-\mathbf{1}(\mathbf{1}^{\top}g)/p$. Both corrections compute their class sums in float64 and cast the result back to float32. Derivative errors are relative $L_2$ errors against the accurate float64 derivative evaluated on the identical float32 logits. The readout-gradient reference casts this logit derivative to float32 before backpropagating through the shared float32 network. At the fixed seed-$4$, step-$16{,}000$ audit, target repair reduces relative derivative error from $32.02\%$ to $4.19\times10^{-7}$, whereas projection leaves $31.88\%$. At step $20{,}000$, projected derivatives retain $52.94$--$86.07\%$ relative error despite a class-sum residual below $2.1\times10^{-8}$ relative to the reference norm. Thus restoring the class-sum identity can prevent the observed failures over this window even with substantial remaining derivative error. Numerical feature inflation and this projection control build on \citet{liu2026glitching}.
 
-At step $15{,}000$, stock logit-gradient errors are $10.56$--$21.86\%$ and readout-gradient errors $18.95$--$43.42\%$. Passing a class-common gradient error through the same saved Adam state produces a class-centered update difference: its centered norm fraction is $45.85$--$73.37\%$, versus approximately $3\times10^{-16}$ for scalar SGD. These descriptive norm ratios are nonadditive. Accurate-CE branches also have smaller feature means: original mean norms at step $15{,}000$ are $2.22$--$3.83$ times their matched corrected values.
+At step $15{,}000$, stock logit-gradient errors are $10.56$--$21.86\%$ and readout-gradient errors $18.95$--$43.42\%$. Passing a class-common gradient error through the same saved Adam state produces a class-centered update difference: its centered norm fraction is $45.85$--$73.37\%$, versus $(2.8$--$4.1)\times10^{-16}$ for scalar SGD, at float64 roundoff. These descriptive norm ratios are nonadditive. Accurate-CE branches also have smaller feature means: original mean norms at step $15{,}000$ are $2.22$--$3.83$ times their matched corrected values.
 
 \subsection{Mean amplification and finite-step overshoot}
 \label{app:mean-update}
@@ -133,4 +133,4 @@
 \label{fig:followup-rms}
 \end{figure}
 
-RMS normalization changes the acute locus. In the accurate-CE event, test accuracy falls from $96.208\%$ to $0.884\%$; its logit derivative has relative error $9.48\times10^{-8}$ and no vanished correct-class entries. Updating only the head retains $96.208\%$, updating only embeddings gives $0.940\%$, and freezing the head still gives $0.895\%$. The minimum reaches $0.526\%$ at step $28{,}503$; the stock-RMS minimum is $0.481\%$ at $28{,}540$. Exact update replay passes. Both RMS event directions locally descend and lower loss at scales $0.001$ and $0.01$, then overshoot at full scale. This counterexample bounds the readout/feature-mean account to the measured unnormalized cases and rules out universal prevention by accurate loss arithmetic. The completed horizons support those finite claims; longer runs and population frequencies remain outside their scope.
+In the RMS runs, the embedding update causes the acute loss of accuracy. In the accurate-CE event, test accuracy falls from $96.208\%$ to $0.884\%$; its logit derivative has relative error $9.48\times10^{-8}$ and no vanished correct-class entries. Updating only the head retains $96.208\%$, updating only embeddings gives $0.940\%$, and freezing the head still gives $0.895\%$. The minimum reaches $0.526\%$ at step $28{,}503$; the stock-RMS minimum is $0.481\%$ at $28{,}540$. Exact update replay passes. Both RMS event directions locally descend and lower loss at scales $0.001$ and $0.01$, then overshoot at full scale. This counterexample bounds the readout/feature-mean account to the measured unnormalized cases and rules out universal prevention by accurate loss arithmetic. The completed horizons support those finite claims; longer runs and population frequencies remain outside their scope.
--- a/main.tex
+++ b/main.tex
@@ -9,7 +9,7 @@
 \usepackage[hidelinks]{hyperref}
 \usepackage{url}
 
-\title{Post-Grokking Collapse at the Representation--Readout Interface\\ in Muon-Trained Transformers}
+\title{\raggedright Post-Grokking Collapse at the Representation--Readout Interface\\ in Muon-Trained Transformers}
 
 \author{Anonymous authors\\
 Paper under double-blind review}
@@ -21,33 +21,33 @@
 \maketitle
 
 \begin{abstract}
-Under the standard routing that assigns Muon the hidden weight matrices and AdamW the embeddings and output head, Muon reaches the grokking threshold on modular arithmetic in fewer steps. Every configuration in our original Muon sweep subsequently loses generalization, with training accuracy failing alongside test; AdamW at larger step sizes collapses too. Freezing the interface suppresses collapse in matched branches. At five captured transitions, the readout update is sufficient for the acute loss of accuracy, while fresh linear decoders recover $98.20$--$100\%$ from collapsed representations. The measured pairs fail tests of exact global basis equivalence. Correcting cross-entropy arithmetic prevents joint train/test collapse through step $30{,}000$ in all five matched branches; the original failing updates locally descend the accurate loss and overshoot at their applied scale. An RMS-normalized counterexample instead fails through its embeddings despite accurate arithmetic. Fourier filtering separates failure through the current readout from \emph{circuit masking}, where the task-aligned family solves the task perfectly in isolation and is outvoted by the remainder; rescaling the family restores accuracy. At a captured collapse, dominant-frequency membership is unchanged and the power distribution remains nearly identical. These spectral statistics alone do not establish that a learned circuit still functions.
+Under the standard routing that assigns Muon the hidden weight matrices and AdamW the embeddings and output head, Muon reaches the grokking threshold on modular arithmetic in fewer steps. Every configuration in our original Muon sweep subsequently loses generalization; some also lose training accuracy. AdamW at larger step sizes collapses too. Freezing the interface suppresses collapse in matched branches. At five captured transitions, the readout update is sufficient for the acute loss of accuracy, while fresh linear decoders recover $98.20$--$100\%$ from collapsed representations. The measured pairs fail tests of exact global basis equivalence. Correcting cross-entropy arithmetic prevents joint train/test collapse through step $30{,}000$ in all five matched branches; the original failing updates locally descend the accurate loss and overshoot at their applied scale. An RMS-normalized counterexample instead fails through its embeddings despite corrected cross-entropy gradients. Fourier filtering separates failure through the current readout from \emph{circuit masking}, where the task-aligned family with its retained constant mode solves the task perfectly and is outvoted in the full model; rescaling the family restores accuracy. At a captured collapse, dominant-frequency membership is unchanged and the power distribution remains nearly identical. These spectral statistics alone do not establish that a learned circuit still functions.
 \end{abstract}
 
 \section{Introduction}
 \label{sec:intro}
 
-Mechanistic accounts of grokking rest on two progress measures: the set of Fourier frequencies a circuit occupies and the fraction of representational power they carry \citep{nanda2023progress}. Together they are the standard certificate that a circuit is present. We exhibit a regime in which the certificate and the function come apart. At the step a grokked transformer stops computing modular addition, the dominant-frequency membership of the task-aligned family is unchanged and its power distribution retains a cosine similarity of $0.9899$ (Figure~\ref{fig:collapse}). This spectral stability raises a natural hypothesis: the representation has changed basis while the readout has failed to compensate. We test that hypothesis directly. Fresh decoders recover the task, but measured residuals fail exact global basis-equivalence tests. Preserved task information and preserved spectral statistics therefore require separate tests of the mechanism.
+Mechanistic studies of grokking combine Fourier analysis with functional interventions. \citet{nanda2023progress} identify a Fourier algorithm and track its formation with restricted and excluded losses. We study what happens to such representations after generalization. At a captured collapse on modular addition, the dominant-frequency membership of the task-aligned family is unchanged and its pair-power distribution retains a cosine similarity of $0.9899$ (Figure~\ref{fig:collapse}). These spectral statistics remain stable as accuracy fails. One possible explanation is that the representation changes basis while the readout fails to compensate. We test this directly: fresh decoders recover the task, while measured residuals fail exact global basis-equivalence tests. The results separate retained task information, accuracy through the current readout, and coordinate equivalence.
 
 \begin{figure}[t]
 \centering
 \includegraphics[width=0.95\textwidth]{figure1_collapse.pdf}
-\caption{A model and its filtered pathway failing, at ten-step evaluation resolution. \textbf{Left:} across a single marked interval, test accuracy falls from $100\%$ to $19.04\%$ and accuracy restricted to the addition family to $24.78\%$, while within the family the set of dominant frequencies is unchanged (Jaccard $1.0000$) and the power distribution across them retains cosine similarity $0.9899$. These spectral statistics remain stable despite the functional failure. \textbf{Right:} the model returns to $99.90\%$ within $300$ steps with the power cosine to the pre-collapse state reaching only $0.55$, having re-solved the task on a different set of frequencies. The addition family is the non-constant $(k,k)$ modes of the two-dimensional Fourier transform of the final residual over the operand grid.}
+\caption{A model and its filtered pathway failing, at ten-step evaluation resolution. \textbf{Left:} test accuracy falls from $100\%$ to $19.04\%$ and addition-filtered accuracy to $24.78\%$. The five dominant addition-frequency pairs are unchanged (Jaccard $1.0000$), and power over all $56$ addition pairs retains cosine similarity $0.9899$. \textbf{Right:} accuracy returns to $99.90\%$ within $300$ steps, with power cosine $0.55$. The same five pairs remain dominant, with a different allocation of power. The addition family consists of the non-constant $(k,k)$ modes of the final residual's operand-grid Fourier transform; accuracy filters also retain the constant mode.}
 \label{fig:collapse}
 \end{figure}
 
 
-The vehicle is Muon \citep{jordan2024muon}, used as deployed at scale \citep{liu2025muon}: hidden weight matrices receive orthogonalized momentum, embeddings and the output head remain with AdamW. Under this routing Muon reaches the grokking threshold on modular arithmetic faster than AdamW \citep{tveit2025muon,wang2026active}, and we reproduce the result. The solutions it reaches do not hold. Every configuration in our stock-arithmetic sweep on $(a+b)\bmod 113$ groks and every one subsequently loses generalization, repeatedly, over hundreds of thousands of steps. Training accuracy falls with test accuracy in the same evaluation interval, so the failure is not a loss of generalization over a solved training set. AdamW at larger step sizes also collapses, with severity differing across optimizers. The instability persists across moduli, widths, training fractions, both operations, and depths 1, 2, and 4. The speedup is itself the instrument: it places a grokked circuit within a few thousand steps, which lets us observe the post-grokking regime repeatedly inside single runs, branch matched trajectories from bit-identical parameter states, and intervene on the circuit at a cost that is prohibitive at AdamW's timescale.
-
-The unnormalized failures localize to the representation--readout interface. The logits depend on the final residual $h$ and the unembedding $W_U$ through $W_Uh$, so $(Rh,W_UR^{-1})$ implements the same function for any invertible $R$ \citep{elhage2021framework}. This parameterization symmetry alone does not specify an optimizer trajectory. Branching from bit-identical states, freezing either side prevents the imminent failure over the tested window. Anchoring the embeddings and readout after circuit formation yields no sub-threshold evaluation across five runs totaling $451{,}400$ post-grokking steps; five paired seeds also remain stable while their unfrozen arms record $137$ to $321$ such evaluations each. Adjacent-checkpoint interventions then isolate the acute readout update, and fresh decoders establish that substantial task information survives it.
-
-Filtering the representation to the Fourier family the task selects separates two failure modes. In \emph{circuit failure}, the filtered family fails through the current readout; this is a pathway-level measurement, compatible with recovery by a fresh decoder. In \emph{circuit masking} it solves the task perfectly while the full model does not: read through the unembedding, the family casts a correct vote on every example, larger on the examples the model answers wrongly, and is outvoted by the rest of the representation. Rescaling the family alone, with nothing retrained, restores the model. In the measured AdamW trajectory, grokking is the same competition resolving upward: the family solves the task in isolation long before the model does, and accuracy tracks its share of representational power.
+We use Muon \citep{jordan2024muon} with the routing used in large-scale training \citep{liu2025muon}: hidden weight matrices receive orthogonalized momentum, while embeddings and the output head remain with AdamW. We reproduce faster grokking under this routing in our modular-arithmetic setup \citep{tveit2025muon,wang2026active}. Every configuration in our stock-arithmetic sweep on $(a+b)\bmod 113$ subsequently loses generalization. Training accuracy falls with test accuracy in captured failure intervals. AdamW at larger step sizes also collapses, with severity differing across optimizers. We observe instability across the tested moduli, widths, training fractions, operations, and depths 1, 2, and 4. Earlier grokking leaves more of the training budget available to study these later failures through matched branches and interventions.
+
+The measured unnormalized failures localize to the representation--readout interface. The logits depend on the final residual $h$ and the unembedding $W_U$ through $W_Uh$, so $(Rh,W_UR^{-1})$ implements the same function for any invertible $R$ \citep{elhage2021framework}. This parameterization symmetry alone does not specify an optimizer trajectory. Branching from bit-identical states, freezing either side prevents the imminent failure over the tested window. Anchoring the embeddings and readout after circuit formation yields no sub-threshold evaluation across five runs totaling $451{,}400$ post-grokking steps; five paired seeds also remain stable while their unfrozen arms record $137$ to $321$ such evaluations each. Adjacent-checkpoint interventions then isolate the acute readout update, and fresh decoders establish that substantial task information survives it.
+
+Filtering the representation to the Fourier family the task selects separates two failure modes. In \emph{circuit failure}, the filtered family fails through the current readout; this is a pathway-level measurement, compatible with recovery by a fresh decoder. In \emph{circuit masking}, the family plus the retained constant mode solves the task perfectly while the full model fails: read through the unembedding, the family casts a correct vote on every example, larger on the examples the model answers wrongly, and is outvoted by the rest of the representation. Rescaling the family alone, with nothing retrained, restores the model. In the measured AdamW trajectory, the family is already highly accurate in isolation before the model generalizes, and model accuracy rises alongside its share of representational power.
 
 Numerical controls connect these events to prior work on softmax collapse and finite-precision loss spikes \citep{prieto2025grokking,liu2026glitching}. Correcting cross-entropy from step $6{,}000$ prevents joint train/test collapse through step $30{,}000$ in five matched branches. The captured failing updates are local descent directions for the accurate loss, but their full applied steps overshoot. Gain-free RMS normalization exposes a boundary: an accurate-loss run still collapses through an embedding update. The numerical contribution and the acute locus therefore depend on the architecture.
 
 \textbf{Contributions.}
 \begin{itemize}\itemsep0pt
-\item \textbf{Phenomenon.} Post-grokking collapse occurs throughout the original Muon sweep and under AdamW at larger step sizes, with joint failures of training and test accuracy (\S\ref{sec:speed}).
+\item \textbf{Phenomenon.} All original Muon sweep configurations lose test accuracy after grokking; joint train/test failures occur in a subset. AdamW also collapses at larger step sizes (\S\ref{sec:speed}).
 \item \textbf{Mechanism.} Matched freezes, adjacent-update interventions, fresh decoders, and arithmetic corrections identify an acute readout failure with surviving task information in the unnormalized model, and an embedding failure in an RMS control (\S\ref{sec:localize}).
 \item \textbf{Taxonomy.} Filtering to the task-selected Fourier family separates circuit failure from circuit masking; masking is a competition on amplitude that rescaling alone resolves, with the reverse transition observed during grokking (\S\ref{sec:outvoted}).
 \item \textbf{Methodology.} Stable spectral statistics can coexist with functional failure. Decoder and basis tests distinguish retained information from exact coordinate equivalence (Figure~\ref{fig:collapse}, \S\ref{sec:outvoted}, Appendix~\ref{app:basis}).
@@ -56,7 +56,7 @@
 \section{Setup}
 \label{sec:setup}
 
-\textbf{Task.} We train on $(a+b)\bmod p$ with $p=113$ throughout, and on $(a-b)\bmod p$ where stated, on the token sequence $[a,b,=]$ with $113$ output classes. A fixed random $30\%$ of the $p^2$ operand grid is the training set, trained full batch with cross-entropy; the original experiments evaluate every $100$ steps. The arithmetic follow-ups use the additional monitoring in \S\ref{sec:arithmetic}.
+\textbf{Task.} We train on $(a+b)\bmod p$, and on $(a-b)\bmod p$ where stated, using token sequences $[a,b,=]$ and $p$ output classes. Defaults are $p=113$ and a fixed random $30\%$ training split of the $p^2$ operand grid. Training is full batch with cross-entropy. Original sweeps evaluate every $100$ steps; diagnostic branches use finer grids where specified. The arithmetic follow-ups add the monitoring in \S\ref{sec:arithmetic}.
 
 \textbf{Architecture.} The model is a decoder-only transformer, $d_{\text{model}}=128$, four heads, MLP width $512$, ReLU, sequence length $3$, every projection bias-free, and no normalization layers. An invertible change of residual basis can be absorbed by the matrices that write to and read from it. This is a symmetry of the parameterization; optimizer updates need not preserve it. The gain-free RMS control restricts this symmetry to $O(d)$. Depth is varied by appending blocks under nested initialization.
 
@@ -76,14 +76,14 @@
 \end{tabular}
 \end{table}
 
-\textbf{Routing and definitions.} Parameters split into the three groups of Table~\ref{tab:routing}; the hidden group is the only one Muon receives. \emph{Sustained $95\%$} is the first evaluation followed by five at or above $95\%$ test accuracy, and grokking speed is always measured against it; \emph{strictly stable} means no post-grokking evaluation below $95\%$, and we report the count and depth of sub-threshold evaluations alongside the label. Matched branches fork from a single in-memory state with zero parameter difference; seed replications run on a bitwise-deterministic backend with the two arms verified identical at every logged evaluation before they are made to differ (Appendix~\ref{app:setup}).
+\textbf{Routing and definitions.} Parameters split into the three groups of Table~\ref{tab:routing}; Muon receives the hidden group. Original grokking speeds use the first of five consecutive evaluations at or above $95\%$ test accuracy. \emph{Strictly stable} means no subsequent evaluation below $95\%$; we also report sub-threshold counts and minima. Matched branches fork from a single in-memory state with zero parameter difference. CPU seed replications have identical logged metrics before the freeze (Appendix~\ref{app:setup}). Follow-up confirmation uses six checks (Appendix~\ref{app:followup-protocol}).
 
 \section{Muon groks faster, and the solutions do not hold}
 \label{sec:speed}
 
-All nine Muon configurations in a $3\times3$ sweep of the hidden group reach sustained generalization, against seven of eleven AdamW configurations, with mean times of $13{,}011$ and $30{,}486$ steps; the mean is computed over AdamW's successes alone, so the factor of $2.34$ is conservative. On the selected settings the advantage is $1.54$ at the median over five seeds, and Muon's grokking step spans one hundred steps where AdamW's spans seven thousand seven hundred. Under a smaller modulus, a smaller training fraction, and half width the ratios grow to $4.9$, and with depth past $5.7$ (\S\ref{sec:depth}).
-
-None of the nine Muon configurations is strictly stable: sub-threshold counts range from $2$ to $592$, post-grokking minima reach $0.78\%$, and the counts have no monotone relationship to either hidden hyperparameter. Four of the seven grokking AdamW configurations also collapse, with severity rising monotonically in the learning rate, from none at $10^{-3}$ to $918$ sub-threshold evaluations at $10^{-2}$. Sweeping the readout learning rate over a factor of five and its weight decay from zero to $0.5$ changes the timing and depth of collapse and eliminates none of it; the zero-decay case removes the weight-decay pull on the readout and leaves the failure in place. Across five seeds neither selected setting is strictly stable: Muon records $137$ to $321$ sub-threshold evaluations on every seed with minima to $16.27\%$, and the AdamW reference records one or two on four of five, with a minimum of $27.59\%$. The instability transfers to modular subtraction with the same asymmetry. Both optimizers collapse, with different severity. Full sweeps, the baseline-selection protocol, and generality variants are in Appendix~\ref{app:sweeps}.
+All nine Muon configurations in a $3\times3$ sweep of the hidden group reach sustained generalization, against seven of eleven AdamW configurations, with mean times of $13{,}011$ and $30{,}486$ steps; the mean is computed over AdamW's successes alone, so the factor of $2.34$ is conservative. On the selected settings the advantage is $1.54$ at the median over five seeds, and Muon's grokking step spans one hundred steps where AdamW's spans seven thousand seven hundred. The ratios reach $4.8$--$4.9$ under half width and a smaller modulus. AdamW never groks in the $20\%$ training variant, giving a lower bound of $6.6$; depth also increases the advantage (\S\ref{sec:depth}).
+
+All nine Muon configurations lose test accuracy after grokking; six have a logged evaluation with both train and test accuracy below $95\%$. None is strictly stable: sub-threshold counts range from $2$ to $592$, post-grokking minima reach $0.78\%$, and the counts have no monotone relationship to either hidden hyperparameter. Four of the seven grokking AdamW configurations also collapse, with severity rising monotonically in the learning rate, from none at $10^{-3}$ to $918$ sub-threshold evaluations at $10^{-2}$. Sweeping the readout learning rate over a factor of five and its weight decay from zero to $0.5$ changes the timing and depth of collapse and eliminates none of it; the zero-decay case removes the weight-decay pull on the readout and leaves the failure in place. Across five seeds neither selected setting is strictly stable: Muon records $137$ to $321$ sub-threshold evaluations on every seed with minima to $16.27\%$, and the AdamW reference records one or two on four of five, with a minimum of $27.59\%$. The tested Muon subtraction run also loses test accuracy; its matched AdamW run stays above threshold. Full sweeps, the baseline-selection protocol, and generality variants are in Appendix~\ref{app:sweeps}.
 
 \section{Localizing the collapse at the interface}
 \label{sec:localize}
@@ -91,14 +91,14 @@
 \begin{figure}[t]
 \centering
 \includegraphics[width=0.7\textwidth]{figure2_quiet.pdf}
-\caption{The $691$ steps before a collapse, at every step. \textbf{Top:} the training loss falls while both gradient norms rise. \textbf{Middle:} the gradient-driven applied step of each group, with the log--log elasticity on its own gradient norm: Muon's does not track the gradient, the two AdamW groups track it more than proportionally. \textbf{Bottom:} on the hidden group the gradient-driven step and the weight decay oppose each other to within two percent, so the net displacement is a small residual, and that residual grows by $64\%$ while the step producing it stays flat.}
+\caption{Every-step measurements over $691$ updates before a collapse. \textbf{Top:} loss and gradient norms in the quiet window. \textbf{Middle:} gradient-driven applied steps and log--log slopes against the hidden gradient norm for Muon and the combined non-hidden norm for both AdamW groups. \textbf{Bottom:} hidden step and decay magnitudes differ by under $2\%$ at step $44{,}100$. Their vector sum is smaller and grows by $64\%$ from step $44{,}100$ to $44{,}700$ while the gradient-driven step changes little.}
 \label{fig:quiet}
 \end{figure}
 
 \subsection{The gradient vanishes and the hidden step does not}
 \label{sec:quiet}
 
-Long before a collapse the model has solved the training set: over the $691$ steps preceding the event we analyze, the training loss sits at $1.5\times10^{-7}$ and the gradient norms on the hidden and non-hidden groups are of order $10^{-6}$ and $10^{-5}$ (Figure~\ref{fig:quiet}). The Newton--Schulz iteration normalizes its input before orthogonalizing and returns singular values near one whatever produced it, so a decoupled step is the premise of the update rule; the window quantifies it and what follows from it. The elasticity of each group's gradient-driven applied step on its own gradient norm, the slope of a log--log regression across the window, is $-0.03$ for the hidden group against $+1.5$ for the embeddings and the readout, with $R^2$ between $0.84$ and $0.98$. AdamW's scale invariance assumes a constant gradient rescaling; here the magnitude drifts over hundreds of steps. On the hidden group the orthogonalized step and the weight decay oppose each other to within two percent, and the net displacement is the residual, which grows by $64\%$ across the window while the step producing it stays flat. The consequence is the finding: the hidden group accumulates $0.073$ of displacement per parameter in root mean square against $0.0091$ for the non-hidden group, a factor of $8.0$, and the instantaneous ratio swings five-fold across the window, which no fixed ratio of learning rates produces. These stock-arithmetic measurements establish different displacement scales (Appendix~\ref{app:quiet}); their causal interpretation requires the numerical controls in \S\ref{sec:arithmetic}. The failure then occupies a single evaluation interval: the training loss rises by a factor of $3.8\times10^{8}$, and training and test accuracy fall together, from $100\%$ to $21.12\%$ and $19.04\%$. Nothing about the orthogonalized step is anomalous at the moment the model breaks.
+Figure~\ref{fig:quiet} measures steps $44{,}010$--$44{,}700$ of an instrumented rerun. Training loss is near $1.5\times10^{-7}$, with hidden and non-hidden gradient norms of order $10^{-6}$ and $10^{-5}$. Muon normalizes its momentum before a fixed Newton--Schulz iteration, reducing sensitivity to its magnitude. Across this window, the log--log slope of the applied hidden step against the hidden gradient norm is $-0.03$. Slopes for the embedding and readout steps against the combined non-hidden norm are about $+1.5$, with $R^2$ across the three fits ranging from $0.84$ to $0.98$. The hidden step and decay largely cancel; their net displacement grows by $64\%$ from step $44{,}100$ to $44{,}700$ while the gradient-driven step changes little. By step $44{,}700$, root-mean-square displacement per parameter from the branch state is $0.073$ for hidden parameters and $0.0091$ for non-hidden parameters, a factor of $8.0$. These measurements establish different displacement scales; their causal interpretation requires the numerical controls in \S\ref{sec:arithmetic}. In the separate ten-step replay used for Figure~\ref{fig:collapse} and Table~\ref{tab:quiet}, training loss rises by $3.8\times10^{8}$ across one interval, and training and test accuracy fall from $100\%$ to $21.12\%$ and $19.04\%$.
 
 \subsection{Which parameters the failure requires}
 \label{sec:branches}
@@ -106,18 +106,18 @@
 \begin{table}[t]
 \centering
 \small
-\caption{Matched branches from a single state at step $44{,}000$, each run to step $46{,}000$. Collapse is recorded when training accuracy falls below $90\%$. Freezing either optimizer-managed side of the interface prevents it over this window; within the auxiliary group only the unembedding does, and hidden matrices together with the unembedding reproduce it.}
+\caption{Matched branches from a single state at step $44{,}000$, each run to step $46{,}000$. Collapse is recorded when training accuracy falls below $90\%$. Freezing either optimizer-managed side of the interface prevents it over this window; among non-hidden parameters only freezing the unembedding does, and hidden matrices together with the unembedding reproduce it.}
 \label{tab:branches}
 \begin{tabular}{lrrrr}
 \toprule
-Branch & First train collapse & Min test & Final test & Max aux.\ grad. \\
+Branch & First train collapse & Min test & Final test & Max non-hidden grad. \\
 \midrule
 \multicolumn{5}{l}{\emph{Group branches}} \\
 Control & $44{,}700$ & $16.77\%$ & $100.00\%$ & $913.07$ \\
 Freeze hidden & none & $99.07\%$ & $99.08\%$ & $2.79\times10^{-5}$ \\
-Freeze auxiliary & none & $100.00\%$ & $100.00\%$ & $0$ \\
-\midrule
-\multicolumn{5}{l}{\emph{Auxiliary component branches}} \\
+Freeze non-hidden & none & $100.00\%$ & $100.00\%$ & $0$ \\
+\midrule
+\multicolumn{5}{l}{\emph{Non-hidden component branches}} \\
 Control & $44{,}710$ & $43.57\%$ & $99.93\%$ & $329.29$ \\
 Freeze token embedding & $44{,}910$ & $34.74\%$ & $99.90\%$ & $3278.60$ \\
 Freeze position embedding & $44{,}760$ & $37.78\%$ & $99.92\%$ & $323.76$ \\
@@ -127,9 +127,9 @@
 \end{tabular}
 \end{table}
 
-Table~\ref{tab:branches} reports branches from a common state, each run for $2{,}000$ steps with a different subset of parameters held fixed. Freezing either group suppresses the failure for the whole window; freezing the token or position embedding delays it without preventing it; freezing the unembedding prevents it over the same window, and leaving only the hidden matrices and the unembedding free reproduces it. With the auxiliary group frozen, Muon continues to apply hidden updates of $0.146$ and hidden displacement nearly doubles while the loss holds at $1.5\times10^{-7}$.
-
-These branches establish suppression over a finite window; freezing also changes subsequent gradients and optimizer history. To isolate the failing update, Figure~\ref{fig:followup}a swaps features and heads between adjacent checkpoints at five captured events. In every seed, the old head retains or improves accuracy on the updated features, while the new head sharply lowers accuracy on the old features. Fresh linear decoders fitted using only training examples recover $98.20$--$100\%$ at selected collapsed checkpoints (Appendix~\ref{app:basis}). Longer branches still show coadaptation: each decodes its own representation and fails on the other's, and initially identical heads reach cosine similarity $-0.028$. These observations establish readout sensitivity and surviving information. Exact global basis equivalence fails for the measured pairs, despite successful recovery of planted transformations. The joint normalization/orthogonalization ablation also has a separate limitation: all six generalizing runs end in non-finite loss (Appendix~\ref{app:ablation}).
+Table~\ref{tab:branches} reports branches from a common state, each run for $2{,}000$ steps with a different subset of parameters held fixed. Freezing either group suppresses the failure for the whole window; freezing the token or position embedding delays it without preventing it; freezing the unembedding prevents it over the same window, and leaving only the hidden matrices and the unembedding free reproduces it. With the non-hidden parameters frozen, Muon continues to apply hidden updates near $0.146$ while loss remains near $1.5\times10^{-7}$.
+
+These branches establish suppression over a finite window; freezing also changes subsequent gradients and optimizer history. To isolate the failing update, Figure~\ref{fig:followup}a swaps features and heads between adjacent checkpoints at five captured events. In every seed, the old head retains or improves accuracy on the updated features, while the new head sharply lowers accuracy on the old features. Fresh linear decoders fitted using only training examples recover $98.20$--$100\%$ at selected collapsed checkpoints (Appendix~\ref{app:basis}). Longer branches still show coadaptation: each decodes its own addition-filtered representation and fails on the other's, and initially identical heads reach cosine similarity $-0.028$. These observations establish readout sensitivity and surviving information. Exact global basis equivalence fails for the measured pairs, despite successful recovery of planted transformations. The joint normalization/orthogonalization ablation also has a separate limitation: non-finite loss was observed in all six generalizing runs (Appendix~\ref{app:ablation}).
 
 
 
@@ -139,7 +139,7 @@
 \begin{figure}[t]
 \centering
 \includegraphics[width=0.84\textwidth]{figure3_seeds.pdf}
-\caption{The intervention at five seeds. Each trajectory is shared until the freeze, with the two arms identical to a maximum absolute difference of zero across every logged evaluation, and then continues once under ordinary Muon and once with the embeddings and unembedding held constant. The unfrozen arm falls below threshold $137$ to $321$ times per seed with minima as low as $16.27\%$; the frozen arm records none and never falls below $95.67\%$.}
+\caption{The intervention at five seeds. Before the freeze, the paired arms have identical logged metrics. One arm continues under ordinary Muon; the other holds embeddings and unembedding constant. The unfrozen arm falls below threshold $137$ to $321$ times per seed with minima as low as $16.27\%$; the frozen arm records none and never falls below $95.67\%$.}
 \label{fig:seeds}
 \end{figure}
 
@@ -165,7 +165,7 @@
 \section{The task selects the family; the update rule shapes its spread}
 \label{sec:family}
 
-Every analysis below views the model's internal state as a function of the operand pair. Running the model on all $p^2$ pairs and applying an orthonormal two-dimensional Fourier transform over the operand axes decomposes any cached state into modes $(k,l)$, which partition into six families: \emph{addition} $(k,k)$, \emph{subtraction} $(k,-k)$, $a$-only, $b$-only, the constant mode, and generic interaction. \emph{Sufficiency} of a family is the accuracy when the state is filtered to it; \emph{necessity} is the accuracy when it is zeroed and everything else retained. The families are disjoint and exhaustive, so the two are exact complements. The full intervention suite is specified in Appendix~\ref{app:fourier}.
+We view each cached state as a function of the operand pair. An orthonormal two-dimensional Fourier transform over all $p^2$ pairs partitions its modes into six disjoint families: non-constant \emph{addition} $(k,k)$, \emph{subtraction} $(k,-k)$, $a$-only, $b$-only, the constant mode, and generic interaction. Family \emph{sufficiency} retains that family's modes; \emph{ablation} removes them. Both interventions retain the constant mode when targeting a non-constant family. Unless stated otherwise, Fourier accuracies and solved-checkpoint selection ($\geq95\%$) use the full operand grid. Appendix~\ref{app:fourier} specifies the interventions.
 
 \begin{table}[t]
 \centering
@@ -187,9 +187,9 @@
 \end{tabular}
 \end{table}
 
-\textbf{One family computes the task, and the task chooses it.} Table~\ref{tab:swap}: the addition family reaches exactly $100.00\%$ in isolation at every solved checkpoint, with no variation by seed, optimizer, or training step, and removing it leaves near chance while removing any other family costs almost nothing. Trained on $(a-b)\bmod 113$ under matched initialization, configuration, and code path, the two families exchange roles exactly: $(k,-k)$ is sufficient everywhere and $(k,k)$ sits at chance to the digit. Nothing in the analysis privileges either family; the task selects it, extending across optimizer choice the universality \citet{chughtai2023toy} report across architectures and seeds.
-
-\textbf{Frequency and phase matter within the family.} Transporting the family onto other diagonal frequencies at matched power gives $2.64\%$. Randomizing phase per conjugate pair gives $0.19\%$, below chance: a coherent phase rotation leaves the model systematically wrong, which is what an algorithm reading the answer off a sum of angles produces when the angles are offset. Randomizing phase per channel gives $0.78\%$. Two branches diverging from a bit-identical state each decode their own representation at $100\%$ and the other's at chance, in both directions: the model relies on particular frequencies, particular phases, and the readout that grew beside them.
+\textbf{The task-aligned family is sufficient at the sampled solved checkpoints.} In Table~\ref{tab:swap}, the addition family reaches $100.00\%$ at every selected addition checkpoint. Removing it leaves near-chance accuracy, while removing the subtraction family has little effect. Under matched initialization, configuration, and code path, training on $(a-b)\bmod113$ exchanges these roles: $(k,-k)$ reaches $100\%$ and $(k,k)$ gives chance accuracy. This consistency across the sampled optimizers and seeds complements prior evidence for shared algorithmic families with variation in the learned circuits \citep{chughtai2023toy}.
+
+\textbf{Frequency and phase matter within the family.} Transporting the family onto other diagonal frequencies at matched power gives $2.64\%$. Randomizing phase per conjugate pair gives $0.19\%$, and randomizing phase per channel gives $0.78\%$. These interventions establish sensitivity to frequency assignment and phase. Two branches diverging from a bit-identical state each decode their own addition-filtered representation at $100\%$ and the other's at chance, showing dependence on the associated readout.
 
 \begin{figure}[t]
 \centering
@@ -198,7 +198,7 @@
 \label{fig:grid}
 \end{figure}
 
-\textbf{The update rule shapes the spread.} On matched runs the Muon representation occupies $326$ effective conjugate pairs of the non-constant spectrum against AdamW's $5$, leaving the task-aligned family holding $28\%$ of non-constant power against AdamW's $91\%$, and the contrast replicates across seeds and inside the family itself (Figure~\ref{fig:grid}; Appendix~\ref{app:dispersion}). Prior evidence for this difference is spectral \citep{ruan2026muon,wang2025tailend,wang2026active}; measuring it in the basis the model computes in reads the spread against the family carrying the algorithm. Ablating the normalization and orthogonalization together collapses the count to $4$, past AdamW's, while leaving the family untouched at $100\%$ sufficiency: the task selects the family, the update rule shapes how widely power spreads, and the two are separable.
+\textbf{The update rule shapes the spread.} On matched runs the Muon representation occupies $326$ effective conjugate pairs of the non-constant spectrum against AdamW's $5$, leaving the task-aligned family holding $28\%$ of non-constant power against AdamW's $91\%$, and the contrast replicates across seeds and inside the family itself (Figure~\ref{fig:grid}; Appendix~\ref{app:dispersion}). These operand-frequency measurements complement prior work on Muon's spectral properties \citep{ruan2026muon,wang2025tailend,wang2026active}. Ablating the normalization and orthogonalization together collapses the count to $4$, past AdamW's, while leaving the family untouched at $100\%$ sufficiency: the task selects the family, the update rule shapes how widely power spreads, and the two are separable.
 
 \section{A circuit can be present, correct, and outvoted}
 \label{sec:outvoted}
@@ -206,15 +206,15 @@
 \begin{figure}[t]
 \centering
 \includegraphics[width=0.85\textwidth]{figure5_modes.pdf}
-\caption{\textbf{Left:} task-aligned family accuracy against full-model accuracy over $98$ checkpoints from the original studies, both evaluated through the current readout. \emph{Circuit failure} denotes failure of this filtered pathway; \emph{circuit masking} sits at the lower right, where the family solves the task and the model does not. At $95$ of $98$ checkpoints the family alone is at least as accurate as the full model, and the three exceptions exceed it by under a point. \textbf{Right:} rescaling the task-aligned component of the masked checkpoint by $\alpha$, with the rest of the representation and the readout untouched.}
+\caption{\textbf{Left:} task-aligned family accuracy (including the retained constant mode) against full-model accuracy over $98$ checkpoints from the original studies, both evaluated through the current readout. \emph{Circuit failure} denotes failure of this filtered pathway; \emph{circuit masking} sits at the lower right, where the family solves the task and the model does not. At $95$ of $98$ checkpoints the family alone is at least as accurate as the full model, and the three exceptions exceed it by under a point. \textbf{Right:} rescaling the non-constant task-aligned component of the masked checkpoint by $\alpha$, with the rest of the representation and the readout untouched.}
 \label{fig:modes}
 \end{figure}
 
-Filtering the final residual to the task-aligned family and decoding measures its accuracy through the current readout; comparison with the unfiltered model measures interference from the remainder. The two measurements separate two failures (Figure~\ref{fig:modes}). In \emph{circuit failure}, filtering gives $2.65\%$ and then chance: this filtered pathway fails, and the family has fallen out of the top twenty power pairs almost entirely. Fresh-decoder recovery in \S\ref{sec:branches} shows why such failure alone cannot establish loss of task information. In \emph{circuit masking}, filtering gives $100\%$ while the full model reaches $45.85\%$: the isolated circuit is perfect, and the representation it sits in overwhelms it. One matched trajectory passes through failure, full recovery, and masking; its frozen counterpart holds pairs, share, and accuracy throughout (Appendix~\ref{app:branchtraj}).
-
-\begin{table}[t]
-\centering
-\caption{Correct-class margin through the unembedding at the masked checkpoint, decomposed exactly by linearity of the bias-free readout; the competitor class is fixed per example as the runner-up under the full model. The final column is the fraction of examples on which the component alone is positive.}
+Filtering the final residual to the task-aligned family and decoding measures its accuracy through the current readout; comparison with the unfiltered model measures interference from the remainder. The two measurements separate two failures (Figure~\ref{fig:modes}). In \emph{circuit failure}, filtering gives $2.65\%$ and then chance: this filtered pathway fails, and the family has fallen out of the top twenty power pairs almost entirely. Fresh-decoder recovery in \S\ref{sec:branches} shows why such failure alone cannot establish loss of task information. In \emph{circuit masking}, filtering gives $100\%$ while the full model reaches $45.85\%$: the isolated circuit is perfect, and the representation it sits in overwhelms it. One matched trajectory passes through failure, full recovery, and masking; its frozen counterpart retains high filtered accuracy and substantial family power (Appendix~\ref{app:branchtraj}).
+
+\begin{table}[t]
+\centering
+\caption{Correct-class margin at the masked checkpoint, decomposed by linearity of the bias-free readout. The competitor is the highest-scoring incorrect class under the full model, fixed across components. The constant mode belongs to the remainder. The final column is the fraction of examples with a positive component margin.}
 \label{tab:margins}
 \begin{tabular}{llrrrr}
 \toprule
@@ -231,34 +231,34 @@
 \end{tabular}
 \end{table}
 
-\textbf{Masking is a competition on amplitude.} Because the unembedding is linear and bias-free, the correct-class margin decomposes exactly into the task-aligned contribution and the remainder (Table~\ref{tab:margins}). The family produces a positive margin on every example in both branches, including every example the masked model answers wrongly, and on those examples its margin is higher, $+6.08$ against $+5.30$. The failures coincide with stronger opposition from the remainder: the remainder contributes negatively on more than nine tenths of examples in both branches, and in the control an opposition of $-6.43$ outvotes a task-aligned term of $+5.72$. Rescaling the task-aligned component by $\alpha$, with nothing retrained, raises the masked model monotonically from $45.85\%$ to $95\%$ at $\alpha=2.55$ and $99.91\%$ at $\alpha=6$: the operative quantity is the ratio of the family's margin to the opposition, and the family's share of representational power is its descriptive proxy.
-
-\textbf{Grokking as masking resolving upward.} Along an AdamW trajectory tracked at thousand-step resolution, the addition family is sufficient at $95.14\%$ at step $3{,}000$, when the model scores $0.30\%$ and the family holds $1.05\%$ of non-constant power; its complement never computes the task at any point in training. Which algorithm the representation contains is settled long before generalization. What changes is the share, rising to $70.62\%$ at the test-accuracy jump, and the concentration within the family. The accuracy transition follows the rising share: the cleanup phase of \citet{nanda2023progress} is this quantity rising, circuit-masking collapse is the same quantity falling, and the two transitions are one measurement run in opposite directions. Neither requires weight decay on the readout, which the zero-decay sweep of \S\ref{sec:speed} removes.
-
-\textbf{What the standard measures report.} Exact nonzero Fourier support is invariant under an invertible residual-basis map; power and its rankings are invariant under orthogonal maps. General invertible maps can change dominant-frequency membership. A joint, compensated basis change also preserves predictions. At the collapse in Figure~\ref{fig:collapse}, the measured dominant-frequency set has Jaccard $1.0000$ and power cosine $0.9899$, while the isolated pathway's mean margin moves from $+18.14$ to $-46.85$. Over this ten-step interval the unembedding moves by $1.285$ of its norm and the representation by $0.392$; the old head on new features scores $26.31\%$, against $5.74\%$ for the reverse. Adjacent-update swaps provide finer localization (Figure~\ref{fig:followup}a). Recovery changes the frequency allocation: accuracy returns to $99.90\%$ within $300$ steps, while the old representation remains poorly decoded by the recovered head over the measured window. Similarly, the frozen branch's addition component scores $99.99\%$ through its own head and $5.05\%$ through the control's. These cross-readout failures show coadaptation; exact basis drift requires the separate tests in Appendix~\ref{app:basis}.
+\textbf{Masking is a competition on amplitude.} Because the unembedding is linear and bias-free, the correct-class margin decomposes exactly into the task-aligned contribution and the remainder (Table~\ref{tab:margins}). Against the full model's highest-scoring incorrect class, the non-constant family contributes a positive margin on every example in both branches, including every example the masked model answers wrongly, and on those examples its margin is higher, $+6.08$ against $+5.30$. The failures coincide with stronger opposition from the remainder: the remainder contributes negatively on more than nine tenths of examples in both branches, and in the control an opposition of $-6.43$ outvotes a task-aligned term of $+5.72$. Rescaling the task-aligned component by $\alpha$, with nothing retrained, raises the masked model monotonically on the sampled grid from $45.85\%$ to at least $95\%$ at $\alpha=2.55$ and $99.91\%$ at $\alpha=6$: the intervention changes the balance of the two logit contributions without retraining. Family power share is a descriptive measure of this change.
+
+\textbf{Family power during grokking.} Along an AdamW trajectory sampled every thousand steps, the addition family reaches $95.14\%$ at step $3{,}000$, when the model scores $0.30\%$ and the family holds $1.05\%$ of non-constant power. Ablating the family leaves near-chance accuracy at every sampled checkpoint. Its share rises to $70.62\%$ at the test-accuracy jump, alongside greater concentration within the family. This trajectory is consistent with the cleanup account of \citet{nanda2023progress}. Power share provides a descriptive measure of that transition; the margin decomposition and rescaling intervention establish amplitude competition at the masked checkpoint.
+
+\textbf{Spectral stability and functional change.} Exact nonzero Fourier support is invariant under an invertible residual-basis map; power and its rankings are invariant under orthogonal maps. General invertible maps can change dominant-frequency membership. A jointly compensated basis change preserves predictions. At the collapse in Figure~\ref{fig:collapse}, dominant-frequency Jaccard is $1.0000$ and power cosine is $0.9899$, while the filtered pathway's mean margin changes from $+18.14$ to $-46.85$. Over this interval the unembedding moves by $1.285$ of its norm and the addition-filtered residual by $0.392$; the old head on new features scores $26.31\%$, against $5.74\%$ for the reverse. Adjacent swaps provide finer localization (Figure~\ref{fig:followup}a). Accuracy returns to $99.90\%$ within $300$ steps with a changed power allocation. In the depth-4 pair, patching the frozen branch's addition component of the final MLP write into the control gives $99.99\%$ with the frozen head and $5.05\%$ with the control head. These results show readout sensitivity; exact basis equivalence requires the separate tests in Appendix~\ref{app:basis}.
 
 \section{Construction and alignment separate in depth}
 \label{sec:depth}
 
-Depth grows the speed advantage, from $2.22\times$ at depth 2 to past $5.7\times$ at depth 4, where the AdamW baseline never sustains generalization, and it removes the condition that separated the optimizers: the AdamW configuration stable at depth 1 records $556$ sub-threshold evaluations at depth 2. Stage-level interventions locate the code. Frequency- and phase-specific sensitivity first appears at an MLP activation in ten of eleven solved checkpoints and at no attention write, while every earlier stage is computationally necessary without carrying the code, which is why ablation alone cannot locate a circuit. Decoding a post-block residual directly with the unembedding reaches $95\%$ only at the final block in all $41$ generalizing checkpoints and never exceeds $17.97\%$ earlier: an MLP in the penultimate block writes the code and the final block makes it legible, so adding depth adds stages whose residual states are not aligned with the readout and does not distribute the alignment across them. Compatibility with the final readout is concentrated at the last block; anchoring the interface at depth 4 cuts sub-$90\%$ evaluations twenty-fold on an exact-state matched branch (Appendix~\ref{app:depth}).
+The speed advantage is $2.22\times$ at depth 2 and exceeds $5.7\times$ at depth 4, where AdamW never sustains generalization within the budget. The AdamW configuration stable at depth 1 records $556$ sub-threshold evaluations at depth 2. Averaging five phase perturbations at each stage, the first accuracy loss exceeding half the baseline occurs at an MLP activation in ten of eleven solved checkpoints and an MLP write in the remaining one. Earlier attention writes can be necessary under zeroing while insensitive to these frequency and phase interventions. This distinction limits localization by ablation alone. Across $41$ checkpoints selected by full-grid accuracy, direct decoding of a post-block residual reaches $95\%$ only at the final block and never exceeds $17.97\%$ earlier. Thus compatibility with the final readout is concentrated at the last block in the sampled models. At depth 4, anchoring the interface cuts sub-$90\%$ evaluations about twenty-fold on an exact-state matched branch (Appendix~\ref{app:depth}).
 
 \section{Related work}
 \label{sec:related}
 
-\textbf{Grokking on modular arithmetic.} \citet{power2022grokking} introduced the phenomenon; \citet{nanda2023progress} reverse-engineered the Fourier algorithm and the progress measures this paper tests, establishing that the circuit forms before the accuracy jump and that the jump is a cleanup phase. \citet{zhong2023clock} show hyperparameters select qualitatively different algorithms; \citet{chughtai2023toy} establish universality across architectures and seeds.
+\textbf{Grokking on modular arithmetic.} \citet{power2022grokking} introduced the phenomenon. \citet{nanda2023progress} reverse-engineered a Fourier algorithm, used causal ablations, and introduced restricted and excluded losses to track circuit formation and cleanup. \citet{zhong2023clock} show that hyperparameters select qualitatively different algorithms; \citet{chughtai2023toy} find shared algorithmic structure alongside variation across learned circuits.
 
 \textbf{Failure after grokking.} \citet{thilak2022slingshot} report cyclic instability under adaptive optimizers; \citet{varma2023explaining} describe ungrokking under reduced data; \citet{prieto2025grokking} identify softmax collapse and a loss-minimization direction that scales logits. \citet{liu2026glitching} connect finite-precision violations of the logit-gradient class-sum identity to feature inflation and loss spikes, and test projection-based corrections. Our arithmetic controls build on this account; adjacent swaps and fresh decoders locate the acute functional failure and surviving information. Joint train/test collapse differs from the perfect-training regime of anti-grokking \citep{prakash2025grokking}, while the accurate-loss RMS event establishes a separate architectural boundary.
 
-\textbf{Muon.} \citet{jordan2024muon} introduced the optimizer and \citet{liu2025muon} deployed it at scale under the routing used here; \citet{tveit2025muon} and \citet{wang2026active} report faster grokking, the latter isolating orthogonalization as the cause and five Newton--Schulz iterations as the robust choice over short horizons; the collapse here occurs at those five iterations over longer ones. \citet{ruan2026muon} and \citet{wang2025tailend} report higher effective rank and a more isotropic spectrum under Muon; we measure the same difference in the basis the model computes in and implicate the normalized-orthogonalization path by ablation.
-
-\textbf{Representation and readout.} \citet{elhage2021framework} state the residual stream's basis freedom, which motivates our explicit equivalence tests. \citet{anthes2023diagnosing} identify readout misalignment as a major component of forgetting under new tasks; \citet{chou2026twospeeds} and \citet{gomezjurado2026longdelay} decompose the approach to grokking into representation and readout speeds. Here the data and task stay fixed, and the dissociation occurs after generalization. Jointly failing training and test predictions can coexist with high accuracy under a fresh decoder.
+\textbf{Muon.} \citet{jordan2024muon} introduced the optimizer and \citet{liu2025muon} deployed it at scale under the routing used here. \citet{tveit2025muon} and \citet{wang2026active} report faster grokking in their tested settings; the latter studies orthogonalization and the speed/stability tradeoff with iteration count, including accuracy dips. We examine the five-iteration configuration over longer horizons. \citet{ruan2026muon} and \citet{wang2025tailend} report higher effective rank and a more isotropic weight spectrum under Muon. Our Fourier measurements concern dispersion over operand frequencies, and the joint ablation tests the contribution of normalization and orthogonalization.
+
+\textbf{Representation and readout.} \citet{elhage2021framework} describe residual-stream basis freedom. \citet{anthes2023diagnosing} identify readout misalignment as a major component of forgetting under new tasks. \citet{chou2026twospeeds} study relative representation and readout learning speeds, while \citet{gomezjurado2026longdelay} finds a decoder bottleneck in one-step Collatz prediction. Here the task stays fixed and the dissociation occurs after generalization: joint train/test failure can coexist with high accuracy under a fresh decoder.
 
 \section{Discussion}
 \label{sec:discussion}
 
-The tests establish surviving task information, with training-only decoders reaching $98.20$--$100\%$, while rejecting exact global basis equivalence for the measured pairs. Arithmetic corrections suppress joint failures over fixed horizons; captured updates overshoot accurate local descent directions. Together with adjacent swaps and the feature-mean intervention, these results support a numerical contribution to acute readout failure. A unique mediator of the accumulated trajectory remains unresolved.
-
-The accurate-loss RMS embedding failure limits this account. The main controls cover five selected trajectories; the operation and normalization extensions use one seed each. Longer horizons and brief test-only excursions remain unresolved, and AdamW collapses limit attribution specifically to Muon. For interpretability, spectral stability, linear decodability, and accuracy through the current readout are distinct measurements. Assessing whether a learned circuit still works requires all three.
+The tests establish surviving task information, with training-only decoders reaching $98.20$--$100\%$, while rejecting exact global basis equivalence for the measured pairs. Arithmetic corrections suppress joint failures over fixed horizons; captured updates overshoot accurate local descent directions. Together with adjacent swaps and the feature-mean intervention, these results support a numerical contribution to acute readout failure. The relative contributions of feature growth and optimizer history remain unresolved.
+
+The accurate-loss RMS embedding failure limits this account. The main controls cover five selected trajectories; the operation and normalization extensions use one seed each. Longer horizons and brief test-only excursions remain unresolved, and AdamW collapses limit attribution specifically to Muon. Fourier filtering identifies functional components using the complete task grid; it does not by itself identify the full weight-level algorithm. Spectral statistics, linear decodability, and accuracy through the current readout answer different questions about what survives collapse.
 
 \subsubsection*{Reproducibility statement}
 The original experiments run on a single device from released code; their repository (anonymized for review) contains the training and analysis scripts, $191$ CSV run logs, and $20$ HTML summaries. Follow-up artifacts add raw diagnostics, model/optimizer/random-state checkpoints, replay checks, and the scripts for every new table and figure. Matched freezes and arithmetic branches verify shared states before intervention. Appendix~\ref{app:setup} specifies the original setup; Appendix~\ref{app:followup} gives the decoder, basis, arithmetic, and architecture-control protocols and their fixed horizons.
--- a/references.bib
+++ b/references.bib
@@ -162,7 +162,7 @@
 
 @article{liu2026glitching,
   title   = {Grokking or Glitching? How Low-Precision Drives Slingshot Loss Spikes},
-  author  = {{Liu Hanqing} and Cao, Jianjun and Li, Yuanze and Zhou, Zijian},
+  author  = {Liu, Hanqing and Cao, Jianjun and Li, Yuanze and Zhou, Zijian},
   journal = {arXiv preprint arXiv:2605.06152},
   year    = {2026},
   url     = {https://arxiv.org/abs/2605.06152}
--- a/tables/FIG_depth.tex
+++ b/tables/FIG_depth.tex
@@ -1,14 +1,13 @@
 \begin{figure}[t]
 \centering
 \includegraphics[width=\textwidth]{figure6_depth.pdf}
-\caption{A depth-$4$ Muon checkpoint. Left: accuracy after intervening on each stage in
-forward order. Relocating the task-aligned family onto other frequencies and scrambling its
-phase leave the model above $99\%$ at every stage through the block-$3$ attention write and
-destroy it at the block-$3$ MLP activation; the two coincide before the cliff. Zeroing a stage destroys
-accuracy everywhere, so earlier stages are computationally necessary while carrying no
-frequency- and phase-specific code. Right: decoding a post-block residual directly with the
-unembedding, for the state itself and for its task-aligned part alone. Neither is legible
-before the final block, and the task-aligned part is the more legible of the two wherever
-both are partial.}
+\caption{Depth-$4$ Muon at step $53{,}000$. \textbf{Left:} the reported stage-intervention
+realization. Relocation and phase scrambling first cause a large drop at the block-$3$ MLP
+activation. The two stochastic curves use replicate $4$ of five and illustrate sensitivity;
+individual later-stage outcomes vary across perturbations. Zeroing the earlier block-$2$ and
+block-$3$ attention writes also destroys accuracy, despite their low sensitivity to these
+Fourier perturbations. \textbf{Right:} direct decoding through the final unembedding, using
+each post-block residual or its addition-filtered component. Both reach high accuracy only
+at the final block.}
 \label{fig:depth}
 \end{figure}
--- a/tables/configs.tex
+++ b/tables/configs.tex
@@ -1,7 +1,7 @@
 \begin{table}[t]
 \centering\small
 \caption{Selected configurations. Stable Muon and Muon are the same configuration up to the
-freeze; their trajectories differ only through backend nondeterminism, which
+freeze; their pre-freeze trajectories can differ through backend nondeterminism, which
 Section~\ref{sec:prevent} reports.}
 \label{tab:configs}
 \begin{tabular}{llll}
--- a/tables/definitions.tex
+++ b/tables/definitions.tex
@@ -1,14 +1,14 @@
 \begin{table}[t]
 \centering\small
 \caption{Operational definitions. Evaluations occur every $100$ steps, so a sustained
-criterion requires the threshold to hold across a $500$-step window.}
+criterion requires the threshold to hold across a $400$-step span.}
 \label{tab:definitions}
 \begin{tabular}{ll}
 \toprule
 Term & Definition \\
 \midrule
-Sustained $99.9\%$ train & First evaluation followed by five evaluations at or above $99.9\%$ train accuracy \\
-Sustained $95\%$ test & First evaluation followed by five evaluations at or above $95\%$ test accuracy \\
+Sustained $99.9\%$ train & First of five consecutive evaluations at or above $99.9\%$ train accuracy \\
+Sustained $95\%$ test & First of five consecutive evaluations at or above $95\%$ test accuracy \\
 Memorization plateau & Sustained-$95$ test step minus sustained-$99.9$ train step \\
 Strictly stable & No post-grokking evaluation below $95\%$ test accuracy \\
 \bottomrule
--- a/tables/depthreadout.tex
+++ b/tables/depthreadout.tex
@@ -2,7 +2,7 @@
 \centering\small
 \caption{Direct-readout accuracy by post-block residual, decoding with the unembedding and
 skipping the remaining blocks. Values are ranges over checkpoints at which the model
-generalizes.}
+has at least $95\%$ full-grid accuracy.}
 \label{tab:depthreadout}
 \begin{tabular}{llrr}
 \toprule
--- a/tables/families-result.tex
+++ b/tables/families-result.tex
@@ -1,8 +1,8 @@
 \begin{table}[t]
 \centering\small
 \caption{Family interventions at forty-three solved checkpoints, spanning five seeds and the
-Muon, Stable Muon, and AdamW regimes. Sufficiency retains only the named family; ablation
-removes it and retains the rest. The chance rate is $0.88\%$.}
+Muon, Stable Muon, and AdamW regimes. Sufficiency retains the named family and constant mode; ablation
+removes the named non-constant family and retains the rest. Accuracies and selection use the full grid. The chance rate is $0.88\%$.}
 \label{tab:families-result}
 \begin{tabular}{lrrrr}
 \toprule
--- a/tables/generality.tex
+++ b/tables/generality.tex
@@ -2,7 +2,7 @@
 \centering\small
 \caption{Controlled variation of modulus, training fraction, and width. Stable Muon and
 Muon share a configuration up to the freeze, so differences in their grokking step measure
-run-to-run variation on this backend rather than an effect of the intervention.}
+pre-intervention run-to-run variation on this backend.}
 \label{tab:generality}
 \begin{tabular}{llrrl}
 \toprule
--- a/tables/interventions.tex
+++ b/tables/interventions.tex
@@ -1,17 +1,18 @@
 \begin{table}[t]
 \centering\small
-\caption{Interventions applied to a cached state. Each is designed to hold one property of
-the representation fixed while destroying another, so that a drop in accuracy attributes
-to the property destroyed.}
+\setlength{\tabcolsep}{5pt}
+\caption{Interventions applied to a cached state. Preserved and altered quantities specify the
+intervention; an accuracy change measures sensitivity to that perturbation. Non-constant-family
+sufficiency and ablation both retain the constant mode.}
 \label{tab:interventions}
 \begin{tabular}{lll}
 \toprule
 Intervention & Preserved & Destroyed \\
 \midrule
-Family sufficiency & Modes in one family & All other families \\
+Family sufficiency & Selected family and constant mode & Other non-constant modes \\
 Family ablation & All other families & Modes in one family \\
 Equal-power relocation & Total power, family membership & Which frequencies carry it \\
-Random native subset & Family membership, power scale & Which of the used frequencies survive \\
+Random native subset & Pair count, native coefficients & Which of the used frequencies survive \\
 Pair-global phase scramble & Support and power exactly & Relative phase across pairs \\
 Channelwise phase scramble & Support and power exactly & Relative phase across channels \\
 Cross-readout substitution & Both representations intact & Pairing of state with its own readout \\
--- a/tables/nons.tex
+++ b/tables/nons.tex
@@ -2,7 +2,7 @@
 \centering\small
 \caption{Ablated runs that reach sustained generalization. Minimum is the lowest test
 accuracy from the sustained-$95$ step to the last recorded evaluation. Terminal is the step
-at which the loss becomes non-finite.}
+at which non-finite loss was observed during the original run.}
 \label{tab:nons}
 \begin{tabular}{llrrrr}
 \toprule
--- a/tables/quiet.tex
+++ b/tables/quiet.tex
@@ -2,9 +2,9 @@
 \centering\small
 \caption{Diagnostics through the quiet window and across the collapse. The training loss
 and both gradient norms are numerically negligible until the failure. The Muon update column
-is the orthogonalized step before weight decay, constant to within three percent; the net
-displacement after decay is smaller and grows across the window, and is reported in the
-text.}
+is the orthogonalized step before weight decay, varying by under three percent across
+the pre-collapse rows. These rows come from the ten-step spectral replay; the net-step
+diagnostics in the text use the separate instrumented rerun.}
 \label{tab:quiet}
 \begin{tabular}{lrrrr}
 \toprule
--- a/tables/sparsity.tex
+++ b/tables/sparsity.tex
@@ -1,7 +1,7 @@
 \begin{table}[t]
 \centering\small
 \caption{Spectral dispersion at solved checkpoints of the matched depth-$1$ runs. Effective
-pairs is the inverse participation ratio over all $6{,}384$ non-constant conjugate pairs,
+pairs is $\exp(-\sum_i q_i\log q_i)$, with normalized pair powers $q_i$, over all $6{,}384$ non-constant conjugate pairs,
 reported as a mean with range. Family power is the task-aligned family's share of
 non-constant power, meaning $(k,k)$ for addition and $(k,-k)$ for subtraction, and top-five
 power is the share carried by the five strongest pairs of the spectrum. The final row ablates the normalization and orthogonalization together.}
--- a/tables/stable.tex
+++ b/tables/stable.tex
@@ -8,7 +8,7 @@
 \label{tab:stable}
 \begin{tabular}{lrrrrr}
 \toprule
-Run & Grokked & Frozen & Post-grokking steps & Minimum & Evals $<95\%$ \\
+Run & Grokked & \shortstack{First frozen\\eval.} & Post-grokking steps & Minimum & Evals $<95\%$ \\
 \midrule
 Main condition & $5{,}900$ & $8{,}100$ & $94{,}100$ & $95.34\%$ & $0$ \\
 Selected Stable Muon & $5{,}400$ & $7{,}500$ & $94{,}600$ & $95.74\%$ & $0$ \\
--- a/tables/stagewise.tex
+++ b/tables/stagewise.tex
@@ -2,8 +2,8 @@
 \centering\small
 \caption{Stage-level interventions at depth $4$, step $53{,}000$, baseline $99.30\%$.
 Relocation transports the addition family onto other diagonal frequencies at matched power;
-phase scrambling randomizes the phase of each conjugate pair; zeroing removes the component
-entirely.}
+phase scrambling randomizes the phase of each conjugate pair. The two stochastic columns
+show the reported realization (replicate $4$ of five); zeroing is deterministic.}
 \label{tab:stagewise}
 \begin{tabular}{llrrr}
 \toprule
