# Declared extension: frequency sparsity, ablation, and phase sensitivity

Added after primary results and before computing this extension. These are additional controls requested in response to the mathematical counterexample, not outcomes selected from a wider sweep.

On addition splits 0 through 4, retain DC and prefixes of 1, 2, 3, 5, 10, 20, or all 56 conjugate diagonal pairs. The ordinary one-hot lookup has exactly equal mathematical power in every diagonal pair: each class channel's coefficient magnitude is its training orbit count divided by p. Report ascending-frequency tie breaking and descending numerical power, with the residual numerical power spread. A second lookup stores p/n_c times the one-hot training label, where n_c is that label's training count. This training-only class normalization makes its projected orbit templates exact one-hot vectors. Test all 56 single-pair filters individually as an explicit sparse counterexample, as well as the prefix grid.

For both lookup tables, evaluate family ablation z - Pz + mean(z), matching retained DC in the original intervention. Evaluate 20 fixed-seed replicates each of pair-global phase scrambling, channelwise phase scrambling, and coefficient relocation by a random derangement of the 56 pair indices. Retain conjugate symmetry and DC. Apply controls both to the isolated family and within the full lookup table. Record magnitude/power preservation and accuracy. Use NumPy RNG seeds determined solely by split, lookup kind, control, and replicate. Prefix and phase results are sensitivity diagnostics; their purpose is to determine whether sparse sufficiency, ablation, or phase sensitivity alone exclude a lookup-only predictor.
